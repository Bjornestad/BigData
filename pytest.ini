[pytest]
python_paths = . scripts Docker/prediction-service/SparkML
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
addopts = -v --ignore=tests/test_aggregate_and_predict.py
markers =
    integration: marks tests as integration tests (require running services)
