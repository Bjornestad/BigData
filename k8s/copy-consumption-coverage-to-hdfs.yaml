apiVersion: batch/v1
kind: Job
metadata:
  name: copy-consumption-coverage-to-hdfs
  namespace: bd-bd-gr-08
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: hdfs-uploader
        image: rtdl/hive-metastore:3.1.2
        command:
          - /bin/bash
          - -c
          - |
            set -e

            echo "================================================"
            echo "UPLOADING CONSUMPTION COVERAGE DATA TO HDFS"
            echo "================================================"

            # Configure HDFS
            export HADOOP_HOME=/opt/hadoop-3.3.1
            export PATH=$HADOOP_HOME/bin:$PATH

            cat > $HADOOP_HOME/etc/hadoop/core-site.xml <<'HDFS_CONFIG'
            <?xml version="1.0"?>
            <configuration>
              <property>
                <name>fs.defaultFS</name>
                <value>hdfs://namenode:9000</value>
              </property>
            </configuration>
            HDFS_CONFIG

            echo ""
            echo "📂 Creating HDFS directory..."
            hdfs dfs -mkdir -p /user/hive/warehouse/consumption_coverage_location

            echo ""
            echo "📊 Local file info:"
            ls -lh /data/consumption_coverage_data.parquet

            echo ""
            echo "📤 Uploading consumption coverage data to HDFS..."
            hdfs dfs -put -f /data/consumption_coverage_data.parquet /user/hive/warehouse/consumption_coverage_location/

            echo ""
            echo "✅ Upload complete!"
            echo ""
            echo "📂 HDFS directory contents:"
            hdfs dfs -ls /user/hive/warehouse/consumption_coverage_location/

            echo ""
            echo "💾 File size in HDFS:"
            hdfs dfs -du -s -h /user/hive/warehouse/consumption_coverage_location/

            echo ""
            echo "================================================"
            echo "✅ UPLOAD SUCCESSFUL"
            echo "================================================"

        volumeMounts:
        - name: data-storage
          mountPath: /data
      volumes:
      - name: data-storage
        hostPath:
          path: /home/akris19/BigData/data
          type: Directory
