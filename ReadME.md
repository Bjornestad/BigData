Install Java 8 and SSH (Phase 1).

Download Hadoop 3.3.6 and Hive 3.1.3 (Phase 2 & 3).

Copy the files from the config/ folder you uploaded into their own /usr/local/hadoop/etc/hadoop/ directories.

Edit hadoop-env.sh to match their own Java path.

Run your setup_hive.sql script inside Hive.