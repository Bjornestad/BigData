apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-energy-actual-fetcher
  namespace: bd-bd-gr-08
spec:
  replicas: 1
  selector:
    matchLabels:
      app: realtime-energy-actual-fetcher
  template:
    metadata:
      labels:
        app: realtime-energy-actual-fetcher
    spec:
      containers:
      - name: fetcher
        image: python:3.11-slim
        command: ["/bin/bash", "-c"]
        args:
          - |
            set -e
            echo "Installing dependencies..."
            pip install -q --no-cache-dir requests pandas kafka-python

            echo "Starting actual energy data fetcher..."
            cd /app/scripts
            python3 fetch_realtime_energy.py
        env:
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "kafka-bootstrap:9092"
        - name: KAFKA_TOPIC
          value: "energy_actual"
        - name: FETCH_INTERVAL
          value: "3600"  # 1 hour (energy data is timestamped hourly)
        volumeMounts:
        - name: code
          mountPath: /app
      volumes:
      - name: code
        persistentVolumeClaim:
          claimName: weather-data-pvc
      restartPolicy: Always
