version: '3.8'

services:
  backend:
    image: dummystad/bigdata:backend-latest
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    image: dummystad/bigdata:frontend-latest
    build:
      context: ./frontend
      dockerfile: Dockerfile

  fetcher:
    image: dummystad/bigdata:fetcher-latest
    build:
      context: .
      dockerfile: Dockerfile.fetcher