services:
  fetcher:
    build: .
    env_file:
      - .env
    container_name: weather_fetcher
    working_dir: /app
    command: python fetch_november_test.py
    volumes:
      # Persist parquet output and any data files on your machine
      - ./:/app
    mem_limit: 4g
    memswap_limit: 4g
